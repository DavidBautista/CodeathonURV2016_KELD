<div class="page-header">
  <% if current_user.id == @user.id %>
      <%= link_to edit_user_path(@user), :class => 'btn btn-default' do %>
          <span class="fa fa-pencil"></span>
          <%= _('Edit') %>
      <% end -%>
  <% else %>
      <%= link_to :back, :class => 'btn btn-default' do %>
          <span class="fa fa-list-alt"></span>
          <%= _('Back') %>
      <% end %>
  <% end -%>

  <h1><%= current_user.name %></h1>
</div>


<div class="row">
  <div class="col-xs-3 col-md-2 col-lg-2">
    <%= image_tag "level-#{current_user.level}.png", class: 'img-responsive' %>
  </div>
  <div class="col-xs-9">
    <h3><%= _('Level') %> <%= current_user.level %> - <%= level_name(current_user.level) %></h3>
    <div class="progress" style="width: 250px; max-width: 100%;">
      <span style="position: absolute; left: 25px;"><%= "exp: #{current_user.experience.to_s}/#{exp_for_next_level(current_user.level)}" %></span>

      <div class="progress-bar" role="progressbar" aria-valuenow="70"
           aria-valuemin="0" aria-valuemax="100" style="width:<%= current_user.experience*100/exp_for_next_level(current_user.level) %>%; color: black;">
      </div>

    </div>
    <p><%= _('Registered at: ') %><%= current_user.created_at.to_date.strftime("%d-%m-%Y") %>.</p>
    <p><strong><%= current_user.tests.count %></strong> <%= _('tests done.') %></p>
    <p><strong><%= current_user.questions.count %></strong> <%= _('questions sent.') %></p>
  </div>
</div>

<div class="row mt20">
  <div class="col-xs-12">
    <div class="container">
      <div class="col-md-12">
        <h1><%= _('Badges')%></h1>
        <div class="well">
          <div id="badget-gallery" class="carousel slide">

            <!-- Carousel items -->
            <div class="carousel-inner">
              <div class="item active">
                <div class="row">
                  <div class="col-sm-2"><a href="#x"><img src="<%= image_path 'badge-1.png' %>" alt="Image" class="img-responsive"></a></div>
                  <div class="col-sm-2"><a href="#x"><img src="<%= image_path 'badge-2.png' %>" alt="Image" class="img-responsive"></a></div>
                  <div class="col-sm-2"><a href="#x"><img src="<%= image_path 'badge-3.png' %>" alt="Image" class="img-responsive"></a></div>
                  <div class="col-sm-2"><a href="#x"><img src="<%= image_path 'badge-4.png' %>" alt="Image" class="img-responsive"></a></div>
                  <div class="col-sm-2"><a href="#x"><img src="<%= image_path 'badge-5.png' %>" alt="Image" class="img-responsive"></a></div>
                  <div class="col-sm-2"><a href="#x"><img src="<%= image_path 'badge-6.png' %>" alt="Image" class="img-responsive"></a></div>

                </div>
              </div>
                <!--/row-->
              <!--/item-->
            </div>

          </div>
          <!--/myCarousel-->
        </div>
        <!--/well-->
      </div>
    </div>
  </div>
</div>